import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["bustanulhudaid.wordpress.com"], // tambahkan domain di sini
  },
};

export default nextConfig;
